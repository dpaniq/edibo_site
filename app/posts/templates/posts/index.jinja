{% extends 'base.jinja' %}

{% block content %}
    <ol>
    {% for post in posts %}
        <li>
            <a href="{{ url_for('posts.post_detail', slug=post.slug) }}" >{{ post.title }}</a>
        </li>
    {% endfor %}
    </ol>
{% endblock %}

