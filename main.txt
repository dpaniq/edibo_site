from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask import url_for, request, render_template, redirect

app = Flask(__name__)
app.debug = True
app.config['SECRET_KEY'] = 'askdnsajd1231h2123bh12hbahsdb123'
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:////my_db.db)

# manager = Manager(app)
db = SQLAlchemy(app)

print(app)

@app.route('/')
def index():
    return redirect(url_for('users'))
    return render_template('hello.html', name='`Nickanme from main.py function`')

@app.route('/users/')
def users():
    return render_template('/users/user.html', name='`Nickanme from main.py function`')

@app.route('/profile/<username>')
def profile(username):
    return f'{username}'


def do_the_login():
    return 'LOGIN HERE'


def do_the_register():
    return 'Register here'


@app.route('/login', methods=['GET', 'POST'])
def login():
    if request.method == 'GET':
        return do_the_login()
    else:
        return do_the_register()

@app.route('/register')
def register():
    return 'Register Form'


with app.test_request_context():  # its uses functions and generate canonical links
    print(url_for('index'))
    print(url_for('login'))
    print(url_for('login', next='/', asd='qwe'))
    print(url_for('profile', username=''))

if __name__ == '__main__':
    app.debug = True
    app.run()

    # app.run(host='0.0.0.0')) # for global users





